/**
 * secrets.h.example — committed TEMPLATE for real credentials.
 * secrets.h.example — PLANTILLA comprometida para credenciales reales.
 *
 * Usage / Uso:
 *   1. Copy this file to include/secrets.h
 *      ( cp include/secrets.h.example include/secrets.h )
 *   2. Fill in the real values.
 *   3. Build & flash. include/secrets.h is gitignored and is NEVER committed.
 *
 * Which reader key? The B2B-Core DemoSeeder prints every reader api_key to
 * the console when seeding (./run setup). Use the key of the reader this
 * physical device represents.
 * ¿Qué clave de lector? El DemoSeeder de B2B-Core imprime todos los
 * api_key de lectores al ejecutar ./run setup. Usa la clave del lector que
 * representa este dispositivo físico.
 */
#pragma once

// Wi-Fi access point the reader connects to / Punto de acceso Wi-Fi
#define WIFI_SSID      "YOUR_WIFI_SSID"
#define WIFI_PASSWORD  "YOUR_WIFI_PASSWORD"

// Backend base URL (B2B-Core). No trailing slash.
// URL base del backend (B2B-Core). Sin barra final.
// Local dev example: "http://192.168.1.50:8000" (php artisan serve --host)
#define API_BASE_URL   "http://localhost:8000"

// Static Bearer key of THIS reader, as printed by the B2B-Core seeder.
// Clave Bearer estática de ESTE lector, impresa por el seeder de B2B-Core.
// The key IS the reader identity — the backend never trusts a client-
// supplied reader id. / La clave ES la identidad del lector.
// TASK-004: the backend must have a readers row whose api_key matches
// this value EXACTLY, or every call answers 401 ("reader key rejected"
// on the serial log). Provisioning options: copy the seeder-printed key
// (./run setup re-prints the same keys) or pin your own — see
// docs/PAIRING.md §Prerequisites.
// / TASK-004: el backend debe tener una fila en readers cuya api_key
// coincida EXACTAMENTE con este valor, o toda llamada responde 401.
// Opciones: copia la clave impresa por el seeder (./run setup la
// re-imprime) o fija la tuya — ver docs/PAIRING.es.md §Prerrequisitos.
#define READER_API_KEY "00000000000000000000000000000000"

// Mode-switch password for the serial console (TASK-003). Type it + Enter
// in the Serial Monitor to toggle OPERATION <-> PAIRING at runtime.
// / Contraseña de cambio de modo por la consola serial (TASK-003).
// Escríbela + Enter en el Monitor Serial para alternar OPERACIÓN <->
// EMPAREJAR en ejecución.
// The Serial Monitor is a physical-access (USB) channel: this password is
// an operator gate, not cryptographic protection — pairing still needs an
// admin-armed backend session. Choose something not used elsewhere.
// / El Monitor Serial es un canal de acceso físico: esta contraseña es una
// compuerta de operador, no criptografía — emparejar sigue necesitando una
// sesión armada por un admin en el backend.
#define MODE_PASSWORD  "CHANGE-ME-MODE-PW"
