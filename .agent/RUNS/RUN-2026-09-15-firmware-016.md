# RUN-2026-09-15-firmware-016 — TASK-013 mDNS service discovery

TASK-013 removes the hard-coded backend IP (ADR-015): the devices
discover `_pulse._tcp.local` via the new `lib/PulseDiscovery` (ESPmDNS,
in-core — no new dependency), share the host-tested selection policy in
`PresenceCore/PulseEndpoint.h`, and re-point `EspApiClient` at boot plus
after any transport failure (cooldown-guarded, failed POSTs never
retried). `API_BASE_URL` is now the boot fallback only; no NVS cache was
added (no NVS system exists; discovery-on-failure covers rotation); no
WebSocket migration (the firmware has no WS client — `:8081` is
dashboard-browsers only). Backend half: B2B-Core TASK-043.

Scope is exactly the TASK-013 file list: `PulseEndpoint.h`,
`PulseDiscovery.h`, `common.h` knobs, `EspApiClient` setter/getter,
`src/main.cpp` (`postToBackend`, boot discovery, effective-URL banner),
`station.{h,cpp}` (`discoverPulseServer`, `Station::post` choke point),
both secrets templates, `API_INTEGRATION.md` (+`.es.md`), build
`hce.16`, `test_pulse_endpoint.cpp` + registration.

## Verification (run in this session)
- `pio test -e native` 118/118 (112 before + 6 new).
- `pio run -e esp32dev` SUCCESS (needed a `discoverPulseServer()`
  forward declaration — defined below `setup()`).
- `pio run -e esp32dev-mock` SUCCESS, `-e esp32cam-reader` SUCCESS.
- `pio run -e esp32cam` SUCCESS after changing `Station::post`'s third
  parameter to `const std::string&` (`CapturePayload::contentType()`
  returns `std::string`, not `const char*`).
- Pending: human bench — rotation test + no-advertisement fallback
  (procedure in B2B-Core `docs/MDNS.md`); flash bumps bench to `hce.16`.

## Status
DONE 2026-09-15 — code + docs + memory committed in this run; bench
flash pending (no hardware in agent runs).
