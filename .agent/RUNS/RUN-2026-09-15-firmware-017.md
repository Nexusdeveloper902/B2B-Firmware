# RUN-2026-09-15-firmware-017 — TASK-014 multipart HMAC canonical

TASK-014 fixes the station classify-401 (ADR-017): image posts sign
the multipart canonical (`CapturePayload::classifySigningBody()` /
`captureSigningBody()` — `event_id + image.sha256`, never the wire
bytes PHP cannot see) via the new `EspApiClient::postMultipart()`
(shared `doPost` core) and `Station::postMultipart()` choke point
(same rediscovery rule); classify + capture call sites switched,
tap/associate untouched. Backend half: B2B-Core TASK-044 (ADR-063).
Build `hce.17` → `hce.18`; `API_INTEGRATION.md` (+`.es.md`) auth +
Content-Type bullets updated.

Scope is exactly the TASK-014 file list: `CapturePayload.{h,cpp}`,
`EspApiClient.h`, `station.{h,cpp}`, `include/config.h` (build bump),
`docs/API_INTEGRATION.md` (+`.es.md`),
`test/test_capture_payload.cpp` + registration.

## Verification (run in this session)
- `pio test -e native` 128/128 (125 before + 3 new: both canonicals
  pinned to the shared `sha256('abc')` literal, binding + NUL safety).
- `pio run -e esp32cam` SUCCESS, `pio run -e esp32dev` SUCCESS.
- Backend gates (other repo, same session): targeted 30 green
  (`DeviceHmacAuthTest` +4, `ClassificationTest`, `CaptureFlowTest`),
  `./run quality` PASSED.
- Pending: human bench — reflash to `hce.18`, tap → auto-capture →
  `classify: HTTP 200` (procedure in `docs/CAMERA_STATION.md`);
  `hce.17` keeps 401ing by design until reflashed.

## Status
DONE 2026-09-15 — code + docs + memory committed in this run; bench
flash pending (no hardware in agent runs).
