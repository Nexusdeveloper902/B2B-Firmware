# RUN-2026-09-15-firmware-015 — TASK-012 esp32cam-reader commit

TASK-012 delivered the `esp32cam-reader` env (ADR-014): an AI-Thinker
ESP32-CAM board acting purely as the NFC reader — no camera connected,
no capture logic. Scope is exactly the TASK-012 file list:
`[env:esp32cam-reader]` in platformio.ini (`board = esp32cam`, builds
`src/main.cpp` only, no `camera/` sources, no `esp32-camera` lib,
`-DPRESENCE_READER_IMPL_RC522 -DREADER_ON_CAM_BOARD`,
`monitor_dtr/rts = 0`); `include/config/esp32cam_board.h` (shared RC522
+ LED pins, extracted from `esp32cam.h` — one definition, included by
both CAM configs so the reader map cannot drift from the station's);
`include/config/esp32cam_reader.h` + dispatcher case in
`include/config.h`; `src/main.cpp` (`StationLed` on GPIO33 under
`READER_ON_CAM_BOARD`, banner board line, wiring hint);
`scripts/flash.sh --cam-reader` (`--board cam-reader`);
HARDWARE_SETUP + FLASHING (EN + ES);
`test/test_cam_reader_config.cpp` (compile-time pin-map guard: station
SPI pins, RST un-driven, LED 33, `#error` if DevKit LED/buzzer, shutter
or camera-bus defines leak in). Default env stays `esp32cam`
(ADR-010 unchanged).

## Verification (from TASK-012, not re-run in this session)
- `pio test -e native` 112/112.
- `pio run -e esp32cam-reader` SUCCESS; `nm` shows 0 `esp_camera` /
  `Station::` symbols. `esp32cam`, `esp32dev`, `esp32dev-mock` SUCCESS.
- `DRY_RUN=1 scripts/flash.sh --cam-reader` →
  `pio run -e esp32cam-reader -t upload`.
- Pending: bench flash on a CAM board with the RC522 harness.

## Status
DONE 2026-09-15 — committed + pushed to `origin/main` in this run,
together with the TASK-011 tree (RUN-2026-09-14-firmware-014 already
covers that half). This file closes the missing-RUN gap for TASK-012.
