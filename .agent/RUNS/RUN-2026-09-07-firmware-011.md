# RUN-2026-09-07-firmware-011 — verify last commits (distrust pass) + protocol documentation

## Task
Owner directive: "Document changes with this protocol … please also
look at the latest implementation of the last commits, i dont trust
them as much." Task record: TASK-009-station-unified-firmware.md
(created this run; the refactor itself landed as `4f1f3e9` +
`933e65f`). A runtime PAT was provided with the directive; it was
NOT needed (everything verified repo-local) and was NOT persisted,
printed, or used, per the credential rules.

## Agent Role
Stateless verification + documentation agent. Pre-flight read:
PROJECT.md, TASK-008 record, snapshot-010, ADR-004, RUN-010,
then the full diffs of `d657265`, `4f1f3e9`, `933e65f`.

## Result
COMPLETED — the distrusted commits survive verification; no code
changes were required. Only append-only memory records were added.

## Summary
Re-reviewed both feature commits line-by-line and re-executed every
claimed check myself: 3/3 device envs SUCCESS, native 92/92,
`-DCAMERA_STATION` present in esp32cam build flags and absent in
esp32dev, fresh-checkout guard re-proven by temporarily removing both
real secrets files (both envs still SUCCESS; secrets restored
byte-identical, placeholder-free). Stale-pin sweep confirms `RST 16`,
buzzer-on-4, old maps and `config_camera.h` survive only as explicit
prohibitions or not at all; no SD init anywhere. Findings below are
the only corrections to prior claims — all minor.

## Changes Made
- NO source changes (nothing to fix).
- New memory records: TASK-009, ADR-009, ADR-010, this run record,
  state snapshot; PROJECT.md gained a TASK-009 section (living doc,
  same precedent as the TASK-008 section).

## Files Changed
- `.agent/TASKS/TASK-009-station-unified-firmware.md` (new)
- `.agent/DECISIONS/ADR-009-station-unified-device.md` (new)
- `.agent/DECISIONS/ADR-010-station-default-env.md` (new, supersedes
  ADR-004 §Decision-1 only)
- `.agent/RUNS/RUN-2026-09-07-firmware-011.md` (new, this file)
- `.agent/STATE/SNAPSHOT-RUN-2026-09-07-firmware-011.md` (new)
- `.agent/PROJECT.md` (appended TASK-009 section)

## Verification (all executed this run, not inherited)
- `pio run -e esp32dev -e esp32dev-mock -e esp32cam` → **3/3 SUCCESS**.
- `pio test -e native` → **92 succeeded** (incl. the 2 station tests).
- `pio run -e esp32cam -v | grep -m2 -o -DCAMERA_STATION` → present;
  `esp32dev -v` → 0 matches (dispatcher routes correctly both ways).
- Fresh checkout: real `secrets.h` + `secrets.camera.h` moved away →
  `esp32cam` + `esp32dev` both SUCCESS via example guards; files
  restored (0 `YOUR_` placeholders → genuine credentials intact,
  `git status` clean of accidents).
- `git show --stat` + full-diff review of `d657265` / `4f1f3e9` /
  `933e65f`; `flash.sh` exec bit confirmed `100755` after `933e65f`
  (mode-only commit) and DRY_RUN selector re-checked.
- Hardware-in-the-loop: NOT claimed (owner bench session still
  pending, per hardware-honesty convention).

## Discoveries
1. Prior claim "reader functionality byte-for-byte untouched" (RUN-010)
   is now outdated by design: the READER is untouched, but the STATION
   deliberately extends the tap flow (auto-classify on
   `awaiting_classification`) and adds LED events to uploads. Recorded
   here so the old sentence is not misread.
2. `docs/API_INTEGRATION.md` said classification is "out of scope" —
   true for the reader, now ambiguous for the station; annotated both
   languages to point at CAMERA_STATION.md (done in `4f1f3e9`).
3. `Station::update()` correctness notes (verified by read, not
   hardware): ENTER works during password lockout (empty lines bypass
   the console by design); `refreshStateLed()` re-indicates only on
   change (no blink restart); static `Station` performs no hardware
   I/O at construction.
4. `EspApiClient::post(path, body, contentType)` uses the byte-array
   POST for ALL bodies now — wire-identical for JSON (no NULs),
   required for multipart JPEG. No test covers the transport itself
   (host-untestable, Arduino-only) — accepted gap, same as before.

## Decisions
- ADR-009 (station = one device, config dispatcher, recoverable
  subsystems, separate secrets stay).
- ADR-010 (default env `esp32cam`; supersedes ADR-004 §Decision-1
  only; ADR-004 rationale explicitly re-applied, not contradicted).

## Problems / Blockers
- None. Two standing bench-level risks carried over (not blockers):
  GPIO33 LED polarity assumed active-LOW (cosmetic); full-load rail
  dip never metered (DIAG-CAM proved comms, not brownout margin).

## Remaining Work
- Owner bench session: tap → `awaiting_classification` → auto-capture
  → classify → points (TASK-009 acceptance needs hardware).
- Confirm GPIO33 red-LED polarity on the bench (flip one flag if
  inverted).
- Optional: per-station `readers` row provisioning (shared demo key
  documented as supported).

## Next Agent Notes
- Trust anchor (re-verified this run): native 92/92; 3/3 envs SUCCESS;
  `-DCAMERA_STATION` routes the dispatcher; secrets guards hold.
- Read ADR-009/ADR-010 before touching station wiring, defaults, or
  the tap flow. ADR-004's default clause is dead — do not "restore"
  `esp32dev` as default without a new ADR.
- The PAT from this run's directive must NOT be fished out of chat
  history into files; nothing in this run needed it.
