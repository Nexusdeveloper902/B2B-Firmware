# RUN-2026-09-07-firmware-008 — independent verification audit (no code changes)

## Task
Owner-directed audit ("look at the latest implementation of the last
commits, I don't trust them as much"), executed as a stateless agent from
a fresh clone on 2026-09-07. Companion run: B2B-Core
RUN-2026-09-07-audit-001 and the new ESP32-CAM-CV .agent/ records.

## Agent Role
Stateless verification/audit agent.

## Result
COMPLETED (audit only; the only repo modifications are these append-only
.agent/ records plus TASK-008).

## Summary
The latest firmware commits are truthful. TASK-007 (Bearer auth header)
verified end-to-end from source; its run record's claims reproduced
exactly. The material gap is not dishonesty — it is scope: THIS REPO HAS
NO ESP32-CAM CODE AT ALL. The owner's recycling spec §6/§7 requires the
working camera implementation (ESP32-CAM-CV/firmware/esp32_cam, board
esp32cam, espressif/esp32-camera, AI-Thinker pin map) to be merged here
as the image-acquisition half of the recycling flow. That was never done;
this repo is a pure RC522 reader firmware. Follow-up recorded as TASK-008.

## Changes Made
- None to firmware source. Appended memory only.

## Files Changed
- .agent/RUNS/RUN-2026-09-07-firmware-008.md (this file)
- .agent/TASKS/TASK-008-esp32cam-merge-and-capture-trigger.md
- .agent/STATE/SNAPSHOT-RUN-2026-09-07-firmware-008.md

## Verification (executed fresh this run)
- `pio test -e native` → **68/68 PASSED** (17.9s) — exactly the
  TASK-007 claim. VERIFIED.
- `pio run -e esp32dev` → **SUCCESS** (101.9s, flash 72.4%) — exactly
  the TASK-007 claim. VERIFIED.
- EspApiClient.h: sends `Authorization: Bearer <key>` via addHeader,
  never setAuthorization — the TASK-007 fix is present in source.
- Bearer regression tests (3) present in test/test_auth.cpp and
  registered — passed as part of 68/68.
- Camera audit: `rg -li "camera|esp32-cam|OV2640|ai-thinker"` over src/
  lib/ include/ platformio.ini → **no camera code**. platformio.ini has
  exactly three envs (esp32dev RC522 default, esp32dev-mock, native);
  no esp32cam environment. The only "camera" mention in docs is the
  classify curl in docs/API_INTEGRATION.md (i.e. the backend contract,
  not device code).
- Cross-check with the reference: ESP32-CAM-CV/firmware/esp32_cam
  compiles (after copying its gitignored include/secrets.h from
  .example — a fresh-checkout trap this repo fixed for itself in
  TASK-001 via __has_include; the reference repo does NOT have that
  guard) and its Python suite passes 114/114+1 skipped. The camera
  reference is genuinely working and available to merge.

## Discoveries
- TASK-007's run record is unusually honest: it documents that the
  earlier "Reader key: PROVEN WORKING (TASK-005 evidence)" snapshot
  claim was an overstatement (curl proved the backend row, not the
  device transport) and corrects it. That correction matches the
  source evidence I see.
- No device-side path calls POST /api/v1/recycling/classify: the
  firmware only taps and pairs. Nothing anywhere POSTs captured images
  to the backend (B2B-Core tests/curl/Postman are the only classify
  callers). The camera→backend bridge does not exist in any repo.
- The recycling spec's ENTER-triggered capture (§2/§8) has no
  counterpart here: the mock env's serial input simulates card UIDs,
  not capture requests.

## Decisions
- Audit-only run: no source changes (stateless-agent protocol §6:
  minimize unrelated changes; record follow-ups as task files).
- TASK-008 created for the camera merge + trigger abstraction instead
  of implementing it inside this run.

## Problems / Blockers
- None for the audit.

## Remaining Work
- TASK-008 (OPEN): merge ESP32-CAM firmware, add esp32cam PlatformIO
  env, ENTER-triggered capture behind a CaptureTrigger abstraction,
  wire capture → image upload → B2B-Core classify endpoint.

## Next Agent Notes
- Read TASK-008 first. The reference platformio.ini to preserve is
  ESP32-CAM-CV/firmware/esp32_cam/platformio.ini (board esp32cam,
  monitor_dtr=0/monitor_rts=0 — the comment warns the AI-Thinker
  auto-download circuit bricks the serial monitor otherwise; keep it).
- Re-run trust anchor: `pio test -e native` → 68/68 at f325b2e.
