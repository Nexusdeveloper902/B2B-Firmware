# RUN-2026-09-16-firmware-018 — TASK-015 per-credential HCE keys

Scope: ADR-018 / TASK-015 (see there). One session across B2B-Core
(TASK-049), B2B-App (TASK-003) and this repo.

## Verification (run in this session)
- `pio test -e native`: 140/140. Before: 128. The 12 new tests pin the
  shared vector, the wrap, ENROLL, `6A88`, the proof payloads, and `403`
  parsing.
- `pio run` for `esp32dev`, `esp32dev-mock`, `esp32cam` and
  `esp32cam-reader`: all SUCCESS, with no HCE `#warning`.
- Searched `include`, `src`, `lib`, `test` and `docs` for `HCE_SECRET` and
  `dev-only-prototype`. The only hits left are the historical mention in
  `docs/HCE_PROTOCOL*` ("no shared HCE_SECRET any more") and a comment in
  `Rc522NfcReader.h` saying an old define is ignored.
- Cross-repo, same session:
  - B2B-Core: `./run test` green and `./run e2e` 49/49, driving the same
    wrap and proof format from PHP;
  - B2B-App: unit tests 41 (1 opt-in skip), `assembleDebug` and
    `assembleDebugAndroidTest` built, `lintDebug` clean.

## Not verified
- No bench run: no hardware in this session. Checklist §10 stays open,
  including RF timing with Keystore HMAC and the pairing tap with ENROLL
  (three APDUs).
- The firmware's own HTTP bodies were not replayed against a live backend
  (`scripts/e2e_backend.sh` does not cover HCE). The backend e2e covers
  the same field names and crypto.
