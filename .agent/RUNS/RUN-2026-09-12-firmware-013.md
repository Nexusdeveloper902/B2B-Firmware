# RUN-2026-09-12-firmware-013 — Pulse rebrand (user-visible strings)

Task: productization pass — the product is Pulse; user-visible naming
must follow (Core RUN-2026-09-12-core-036 / ADR-047 is the contract).

## Changes
- station.cpp embedded page: <title>Pulse Station</title>, H1
  "Pulse — ESP32-CAM Station / Estación ESP32-CAM".
- Serial boot banner: "Pulse — NFC Reader / Lector NFC".
- platformio.ini header comment, main.cpp header comment.
- README.md / README.es.md H1s + prose, docs/HARDWARE_SETUP.md + .es.md
  titles.

## Deliberately unchanged
- `namespace Presence` / `Presence::Station` and all internal
  identifiers (not user-visible; renaming = functional churn, Core
  ADR-047 records the decision).

## Verification
- No compile performed (no PlatformIO in this environment; only string
  literals inside C++ raw strings and comments changed — no identifiers
  touched). Grep-verified: zero "Presence Platform"/"Plataforma de
  Presencia" left in user-visible strings or docs.
- Static grep audit only; bench flash/serial check remains for the
  owner's next hardware bench.

## Status
DONE 2026-09-12 — uncommitted tree.
