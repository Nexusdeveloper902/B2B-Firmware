# RUN-2026-09-07-firmware-009 — second independent verification audit (no code changes)

## Task
Owner re-issued the standing audit directive ("look at the latest
implementation of the last commits, I don't trust them as much") in a
fresh session. RUN-2026-09-07-firmware-008 (commit 33434f2, HEAD of
main when this run started) already claimed verification of the
TASK-007 Bearer fix and the repo's test/build state. Per the
stateless-agent protocol (§1.3/§16.5), this run re-verified those
claims independently: clone confirmed synced to origin/main @ 33434f2
(clean tree, clean remote URL), then full re-execution.

## Agent Role
Stateless verification/audit agent — second-opinion run.

## Result
COMPLETED (audit only — zero firmware source changes; the only repo
modifications are these append-only .agent/ records).

## Summary
RUN-2026-09-07-firmware-008 told the truth. Native suite 68/68 and the
esp32dev build reproduced exactly; the Bearer fix is physically in the
source at EspApiClient.h lines 59–60; the camera-absence finding
(this repo has NO ESP32-CAM code) re-confirmed. Together with
RUN-2026-09-07-audit-002 in B2B-Core and RUN-2026-09-07-cv-audit-002
in ESP32-CAM-CV, both same-date independent runs now agree: the recent
commits are honest, and the open work is TASK-008 (camera merge +
ENTER capture trigger), still untouched.

## Changes Made
- None to firmware source. Appended memory only.

## Files Changed
- .agent/RUNS/RUN-2026-09-07-firmware-009.md (this file)
- .agent/STATE/SNAPSHOT-RUN-2026-09-07-firmware-009.md

## Verification (all executed fresh this run; HEAD @ 33434f2)
- `pio test -e native` → **68 test cases: 68 succeeded** (3.59s) —
  exactly the count claimed by firmware-008 (which reported 17.9s;
  duration difference is warm toolchain cache, irrelevant to truth).
  Includes the bearer regression cases (e.g.
  `bearer_value_with_empty_key_is_the_bare_scheme` PASSED).
- `pio run -e esp32dev` → **SUCCESS** (18.87s, esptool image created,
  27 ELF sections merged — claimed 101.9s, again cache warmth).
- Bearer fix in source: EspApiClient.h builds the header explicitly —
  `http.addHeader("Authorization", bearerAuthorizationValue(...))`
  (lines 59–60) with the TASK-007 comment block forbidding
  `setAuthorization()`. VERIFIED in code, not just docs.
- Camera-absence scan (`grep -rli "camera|esp32-cam|OV2640|ai-thinker"`
  over src/ lib/ include/ platformio.ini): NO matches. platformio.ini
  has exactly three environments (esp32dev, esp32dev-mock, native) —
  no esp32cam env. Claim VERIFIED again.
- Repo has zero GitHub Actions runs (API total_count=0) — consistent
  with firmware-008 claiming only local verification.
- Cross-repo (same date): the reference ESP32-CAM-CV/firmware/esp32_cam
  build re-verified green after its gitignored secrets.h copy, and its
  fresh-checkout failure reproduced (see that repo's
  RUN-2026-09-07-cv-audit-002). TASK-008's "known-working reference"
  premise holds.

## Discoveries
- Second-opinion agreement: firmware-008's record is accurate on every
  claim. The Bearer transport story (ADR-007: 401-with-valid-key until
  the explicit header fix) is corroborated by source + passing tests.
- PlatformIO toolchains cached under ~/.platformio make re-verification
  cheap (~23s total vs ~120s claimed cold); durations in run records
  are cache-dependent — compare build/test RESULTS, not seconds.
- (Execution-host observation, recorded in B2B-Core
  RUN-2026-09-07-audit-002 Discovery #3: exported env vars do not
  persist between shell commands on this runner; supply credentials
  per command.)

## Decisions
- Audit-only: no source changes (protocol §1.4; TASK-008 stays the
  implementation vehicle).
- Append-only records pushed to main following this repo's convention
  (firmware-008 did the same; docs-only commit).

## Problems / Blockers
- None.

## Remaining Work
- TASK-008 (OPEN, unchanged): merge ESP32-CAM firmware from the
  reference repo, add the esp32cam env (keep monitor_dtr=0 /
  monitor_rts=0), ENTER-triggered capture behind a CaptureTrigger
  boundary, wire capture → POST /api/v1/recycling/classify with Bearer
  auth, and apply the __has_include secrets guard to the merged
  sources.

## Next Agent Notes
- Re-run trust anchor: `pio test -e native` → 68/68 and `pio run -e
  esp32dev` → SUCCESS on main @ 33434f2 (verified twice now:
  firmware-008 and firmware-009).
- Start from TASK-008; the reference platformio.ini to copy is in
  ESP32-CAM-CV/firmware/esp32_cam (board esp32cam). Do NOT invent a
  board config.
- pio is not preinstalled on the audit host: install via
  `python3 -m pip install platformio` (the venv pip, not system pip —
  system pip refuses PEP 668).
